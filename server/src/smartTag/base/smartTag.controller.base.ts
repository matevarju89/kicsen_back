/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as common from "@nestjs/common";
import * as swagger from "@nestjs/swagger";
import * as nestAccessControl from "nest-access-control";
import * as defaultAuthGuard from "../../auth/defaultAuth.guard";
import { isRecordNotFoundError } from "../../prisma.util";
import * as errors from "../../errors";
import { Request } from "express";
import { plainToClass } from "class-transformer";
import { ApiNestedQuery } from "../../decorators/api-nested-query.decorator";
import { SmartTagService } from "../smartTag.service";
import { AclValidateRequestInterceptor } from "../../interceptors/aclValidateRequest.interceptor";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { SmartTagCreateInput } from "./SmartTagCreateInput";
import { SmartTagWhereInput } from "./SmartTagWhereInput";
import { SmartTagWhereUniqueInput } from "./SmartTagWhereUniqueInput";
import { SmartTagFindManyArgs } from "./SmartTagFindManyArgs";
import { SmartTagUpdateInput } from "./SmartTagUpdateInput";
import { SmartTag } from "./SmartTag";
import { RecipeFindManyArgs } from "../../recipe/base/RecipeFindManyArgs";
import { Recipe } from "../../recipe/base/Recipe";
import { RecipeWhereUniqueInput } from "../../recipe/base/RecipeWhereUniqueInput";
@swagger.ApiBearerAuth()
@common.UseGuards(defaultAuthGuard.DefaultAuthGuard, nestAccessControl.ACGuard)
export class SmartTagControllerBase {
  constructor(
    protected readonly service: SmartTagService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @nestAccessControl.UseRoles({
    resource: "SmartTag",
    action: "create",
    possession: "any",
  })
  @common.Post()
  @swagger.ApiCreatedResponse({ type: SmartTag })
  @swagger.ApiForbiddenResponse({ type: errors.ForbiddenException })
  async create(@common.Body() data: SmartTagCreateInput): Promise<SmartTag> {
    return await this.service.create({
      data: {
        ...data,

        family: data.family
          ? {
              connect: data.family,
            }
          : undefined,
      },
      select: {
        createdAt: true,

        family: {
          select: {
            id: true,
          },
        },

        id: true,
        lang: true,
        name: true,
        updatedAt: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @nestAccessControl.UseRoles({
    resource: "SmartTag",
    action: "read",
    possession: "any",
  })
  @common.Get()
  @swagger.ApiOkResponse({ type: [SmartTag] })
  @swagger.ApiForbiddenResponse()
  @ApiNestedQuery(SmartTagFindManyArgs)
  async findMany(@common.Req() request: Request): Promise<SmartTag[]> {
    const args = plainToClass(SmartTagFindManyArgs, request.query);
    return this.service.findMany({
      ...args,
      select: {
        createdAt: true,

        family: {
          select: {
            id: true,
          },
        },

        id: true,
        lang: true,
        name: true,
        updatedAt: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @nestAccessControl.UseRoles({
    resource: "SmartTag",
    action: "read",
    possession: "own",
  })
  @common.Get("/:id")
  @swagger.ApiOkResponse({ type: SmartTag })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @swagger.ApiForbiddenResponse({ type: errors.ForbiddenException })
  async findOne(
    @common.Param() params: SmartTagWhereUniqueInput
  ): Promise<SmartTag | null> {
    const result = await this.service.findOne({
      where: params,
      select: {
        createdAt: true,

        family: {
          select: {
            id: true,
          },
        },

        id: true,
        lang: true,
        name: true,
        updatedAt: true,
      },
    });
    if (result === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return result;
  }

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @nestAccessControl.UseRoles({
    resource: "SmartTag",
    action: "update",
    possession: "any",
  })
  @common.Patch("/:id")
  @swagger.ApiOkResponse({ type: SmartTag })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @swagger.ApiForbiddenResponse({ type: errors.ForbiddenException })
  async update(
    @common.Param() params: SmartTagWhereUniqueInput,
    @common.Body() data: SmartTagUpdateInput
  ): Promise<SmartTag | null> {
    try {
      return await this.service.update({
        where: params,
        data: {
          ...data,

          family: data.family
            ? {
                connect: data.family,
              }
            : undefined,
        },
        select: {
          createdAt: true,

          family: {
            select: {
              id: true,
            },
          },

          id: true,
          lang: true,
          name: true,
          updatedAt: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @nestAccessControl.UseRoles({
    resource: "SmartTag",
    action: "delete",
    possession: "any",
  })
  @common.Delete("/:id")
  @swagger.ApiOkResponse({ type: SmartTag })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @swagger.ApiForbiddenResponse({ type: errors.ForbiddenException })
  async delete(
    @common.Param() params: SmartTagWhereUniqueInput
  ): Promise<SmartTag | null> {
    try {
      return await this.service.delete({
        where: params,
        select: {
          createdAt: true,

          family: {
            select: {
              id: true,
            },
          },

          id: true,
          lang: true,
          name: true,
          updatedAt: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @nestAccessControl.UseRoles({
    resource: "Recipe",
    action: "read",
    possession: "any",
  })
  @common.Get("/:id/recipe")
  @ApiNestedQuery(RecipeFindManyArgs)
  async findManyRecipe(
    @common.Req() request: Request,
    @common.Param() params: SmartTagWhereUniqueInput
  ): Promise<Recipe[]> {
    const query = plainToClass(RecipeFindManyArgs, request.query);
    const results = await this.service.findRecipe(params.id, {
      ...query,
      select: {
        category1: true,
        category2: true,
        category3: true,
        category4: true,
        createdAt: true,
        description: true,
        difficulty: true,

        family: {
          select: {
            id: true,
          },
        },

        forHowMany: true,
        id: true,
        ingredients: true,

        postedBy: {
          select: {
            id: true,
          },
        },

        title: true,
        updatedAt: true,
      },
    });
    if (results === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return results;
  }

  @nestAccessControl.UseRoles({
    resource: "SmartTag",
    action: "update",
    possession: "any",
  })
  @common.Post("/:id/recipe")
  async connectRecipe(
    @common.Param() params: SmartTagWhereUniqueInput,
    @common.Body() body: RecipeWhereUniqueInput[]
  ): Promise<void> {
    const data = {
      recipe: {
        connect: body,
      },
    };
    await this.service.update({
      where: params,
      data,
      select: { id: true },
    });
  }

  @nestAccessControl.UseRoles({
    resource: "SmartTag",
    action: "update",
    possession: "any",
  })
  @common.Patch("/:id/recipe")
  async updateRecipe(
    @common.Param() params: SmartTagWhereUniqueInput,
    @common.Body() body: RecipeWhereUniqueInput[]
  ): Promise<void> {
    const data = {
      recipe: {
        set: body,
      },
    };
    await this.service.update({
      where: params,
      data,
      select: { id: true },
    });
  }

  @nestAccessControl.UseRoles({
    resource: "SmartTag",
    action: "update",
    possession: "any",
  })
  @common.Delete("/:id/recipe")
  async disconnectRecipe(
    @common.Param() params: SmartTagWhereUniqueInput,
    @common.Body() body: RecipeWhereUniqueInput[]
  ): Promise<void> {
    const data = {
      recipe: {
        disconnect: body,
      },
    };
    await this.service.update({
      where: params,
      data,
      select: { id: true },
    });
  }
}
